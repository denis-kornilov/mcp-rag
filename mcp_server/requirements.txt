# mcp_server (gateway) — dependencies
# In local-mode, gateway imports rag_server directly (in-process),
# therefore all rag_server dependencies are needed.
# embed_server runs as a separate process — its packages (onnxruntime, transformers,
# optimum) are NOT needed here.
# PYTHONPATH must include mcp-rag/ (project root).

mcp>=1.12.4
fastapi[standard]>=0.115.0
uvicorn>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.2.1
requests>=2.31.0
pathspec>=0.12.1

# Vector store (local mode — rag_server in-process)
chromadb==0.5.5

# File system watcher (disabled by default: FS_WATCHER_ENABLED=false)
watchdog>=6.0.0

# Cross-encoder reranker (optional — fallback to lexical overlap if not installed)
# pip install sentence-transformers>=2.7.0
# sentence-transformers>=2.7.0

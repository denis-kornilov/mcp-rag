# embed_server — dependencies
# PyTorch is NOT used. Only ONNX Runtime.
#
# GPU variants (install.sh selects the right one, they conflict with each other):
#   CPU:         pip install onnxruntime
#   NVIDIA CUDA: pip install onnxruntime-gpu
#   AMD ROCm:    pip install onnxruntime-rocm

fastapi[standard]>=0.115.0
uvicorn>=0.30.6
pydantic>=2.9.2
python-dotenv>=1.2.1
requests>=2.31.0
numpy>=1.24.0

# Tokenizer and export to ONNX (without PyTorch inference)
transformers>=4.40.0
optimum[onnxruntime]>=1.21.0
overrides>=7.7.0

# onnxruntime is installed via install.sh depending on GPU
